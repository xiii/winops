---
version: 5

defaults:  
  datadir: hieradata
  data_hash: yaml_data 

hierarchy:
  - name: "Per-node data"                   
    path: "nodes/%{facts.hostname}.yaml"  .

  - name: "Per location defaults"
    path: "location/%{facts.location}.yaml"

  - name: "Common data"
    path: "common.yaml"
